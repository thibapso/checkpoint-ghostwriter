// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Adicione aqui suas configurações do Next.js se precisar.
  // Exemplo:
  // reactStrictMode: true,
};

export default nextConfig;